{
  "name": "chatbot",
  "version": "1.0.0",
  "description": "Fullstack Chatbot using React, Vite, Express, Node, TypeScript, and Gemini API",
  "main": "index.js",
  "author": "Nayan Dixit",
  "license": "ISC",
  "engines": {
    "node": "20.11.1"
  },
  "scripts": {
    "dev": "concurrently -n FRONTEND,BACKEND -c blue,green \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
